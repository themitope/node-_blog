extends layout

block content
	h1= article.title
	small Written by #{author}
	p= article.body
	hr
	if user
		if user.id ==article.author 
			a.btn.btn-outline-secondary(href='/articles/edit/'+article._id) Edit
			a.btn.btn-danger.delete-article(href='#', data-id=article._id) Delete